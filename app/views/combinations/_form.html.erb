<%= form_for(@combination) do |f| %>
  <% if @combination.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@combination.errors.count, "error") %> prohibited this combination from being saved:</h2>

      <ul>
      <% @combination.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>



  <div class="field form-group">
    <%= f.label "Context for the quote" %><br>
    <%= f.text_area :context, placeholder:"Context", class:"form-control" %>
  </div>
  <div class="field form-group">
    <%= f.label "Said on " %><br>
    <%= f.date_select :saidon, placeholder:"Date said", class:"form-control" %>
  </div>
  

    
  
    
  <button type="submit" class="btn btn-default">Save Conversation</button>

<% end %>



